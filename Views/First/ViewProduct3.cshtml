@model AppMvcNet.Models.ProductModel

@{
    var product = ViewBag.product as ProductModel;
}

@* @{
Layout = "_Layout";
} *@
<h1>@ViewData["Title"]</h1>
<div class="card bg-warning text-white">
    <h1 class="card-header">@product.Name</h1>
    <div class="card-body">
        <p class="card-text">
            Price: @product.Price
        </p>
    </div>
</div>
<hr>
@this.Path